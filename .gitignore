# ── 依赖 ──
node_modules/

# ── 环境配置（含密钥，禁止提交） ──
.env

# ── 构建产物 ──
dist/
*.tsbuildinfo

# ── 运行时数据（本地生成，不提交） ──
data/state.json
data/local-library-cache.json
data/cache/

# ── 数据库 ──
*.sqlite
*.sqlite-shm
*.sqlite-wal

# ── 日志 ──
*.log

# ── 操作系统 ──
.DS_Store
Thumbs.db

# ── IDE / 编辑器 ──
.vscode/
.idea/
*.swp
*.swo

# ── AI 工具配置 ──
.qoder
.codex
.claude
claude.md
