# Loop 00 验收记录

日期：2026-06-20

## 已完成

- 建立 `apps/web` 前端工程目录。
- 建立 `apps/server` 后端工程目录。
- 建立 `packages/shared` 共享类型目录。
- 添加 Vite、React、TypeScript、Fastify、Zod 的 package 配置。
- 保留 v0.1 的 `server.js` 和 `public/`，没有直接删除旧能力。

## 依赖状态

运行依赖安装成功：

- `fastify`
- `zod`
- `@fastify/static`
- `@fastify/cors`

开发依赖安装时网络多次超时，`package.json` 和 `package-lock.json` 已写入依赖声明，但 `node_modules` 中仍缺：

- `react`
- `react-dom`
- `@types/node`
- `@types/react`
- `@types/react-dom`
- `concurrently`

后续需要用户在本地执行：

```powershell
npm install
```

或单独补装：

```powershell
npm install -D react react-dom @types/node @types/react @types/react-dom concurrently
```

## 未完成验收

由于依赖安装不完整，以下命令暂未通过完整验收：

```powershell
npm run build
npm run dev:web
```

按用户要求，不继续反复安装依赖，先记录缺口并继续推进可落地的工程文件。
