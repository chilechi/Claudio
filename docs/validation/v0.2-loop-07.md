# Loop 07 验收记录

日期：2026-06-20

## 已完成

- 浏览器 TTS 保持真实可用路径。
- 新增麦克风按钮 `micBtn`。
- 新增浏览器 `SpeechRecognition` / `webkitSpeechRecognition` 语音输入。
- 浏览器不支持 ASR 时明确提示不可用。
- 麦克风只在用户点击按钮后启动，不自动请求权限。
- TTS/ASR provider 状态显示为 `fallback`，不伪装 Fish Audio 或 Whisper 已连接。

## 验收命令

```powershell
node --check public\app.js
```

静态检查：

- `micBtn` 存在。
- `SpeechRecognition` 和 `webkitSpeechRecognition` 检测存在。
- 没有自动调用 `startVoiceInput()`。
- `listening` 样式存在。

Provider 状态：

```json
{
  "tts": "fallback",
  "asr": "fallback"
}
```

## 结论

Claudio 已具备真实浏览器语音输出和可选语音输入能力。真实 TTS/ASR 服务仍在 `docs/v0.2-missing-inputs.md` 中等待配置，不伪装已连接。
